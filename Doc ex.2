#include <stdio.h>
#include <stdlib.h>

void generate_lotto_numbers(int n) {
    FILE *fp;
    fp = fopen("lotto.txt", "w");


    srand(1); 

    fprintf(fp, "========= lotto649 =========\n");
    for (int i = 1; i <= n; ++i) {
        fprintf(fp, "[%d]: ", i);

        int lotto[7]; 
        for (int j = 0; j < 7; ++j) {
            lotto[j] = rand() % 69 + 1;
            fprintf(fp, "%02d ", lotto[j]);
            if (j != 6) {
            }
        }

        fprintf(fp, "\n");
    }

    for (int i = n + 1; i <= 5; ++i) {
        fprintf(fp, "[%d]: __ __ __ __ __ __ __\n", i);
    }

    fprintf(fp, "========= csie@CGU =========\n");

    fclose(fp);
}

int main() {
    int n;
   
    scanf("%d", &m);

    if (m < 1 || m > 5) {
        
        return 1;
    }

    generate_lotto_numbers(m);


    return 0;
}
